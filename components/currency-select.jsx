'use strict';

var React = require('react');

module.exports = React.createClass({
  render: function() {
    return(
      <select>
        <option value="1">GBP</option>
      </select>
    )
  }
});
